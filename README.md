# React Native + Cloudflare + tRPC Starter template 

This is a starter template for building a React Native application with Cloudflare Workers as the backend, using tRPC for type-safe API communication. This setup allows you to create a full-stack application with a seamless developer experience.

We have all the basics setup with authentication, database integration, and API routes. You can easily extend this template to fit your specific needs.
